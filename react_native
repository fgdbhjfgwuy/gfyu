import React from 'react'
import { View } from 'react-native'

import styled from 'styled-components/native'

const Rectangle = (props) => {
  return (
    <View>
      <DummyContainer>
        <StyledRectangle
          source={require('../playground_assets/rectangle0496-kgl9-900h.png')}
          alt="Rectangle0496"
        ></StyledRectangle>
      </DummyContainer>
    </View>
  )
}

export default Rectangle

const DummyContainer = styled('View')({
  width: '100%',
  height: '900px',
})

const StyledRectangle = styled('Image')({
  width: '375px',
  height: '812px',
  'border-color': 'transparent',
  position: 'relative',
})
